apiVersion: v1
kind: Service
metadata:
  name: {{ template "docker-compose.fullname" . }}
  labels:
    app: {{ template "docker-compose.name" . }}
    chart: {{ template "docker-compose.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "docker-compose.name" . }}
    release: {{ .Release.Name }}
